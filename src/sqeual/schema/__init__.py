"""Stage 02: the schema card — introspection, rendering, and slicing.

The card is the ground truth the guard checks a model's SQL against, and the
rendered card is the text the model is shown. Both come from the database
itself; nothing here is maintained by hand in parallel with the DDL.
"""
